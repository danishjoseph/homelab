SERVICE_NAME='stockdog'
PORT='{{ stockdog.port }}'
DB_NAME='{{ stockdog.db_name }}'
DB_USERNAME='{{ stockdog.db_username }}'
DB_PASSWORD='{{ stockdog.db_password }}'
CRON_SCHEDULE='{{ stockdog.cron_schedule }}'
TZ='{{ stockdog.timezone }}'
